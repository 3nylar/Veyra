# CI (§45)
#
# A failed security test MUST fail the build. There is no `continue-on-error`
# on any job below, and no step is marked advisory.
#
# `verify` and `guards` run across ubuntu/windows/macos. This is not
# defensive boilerplate: VEY-001 was a guard that silently checked zero
# files on Windows for months while Linux CI stayed green. A single-OS
# pipeline structurally cannot catch a bug that only exists on another OS.
#
<<<<<<< HEAD
# `e2e` runs the shipped browser pages in real Chromium/Firefox/WebKit via
# Playwright. VEY-020 broke 100% of browser syncs while 919 vitest tests
# stayed green, because the defect was a `fetch` receiver-binding rule that
# neither Node's undici nor jsdom enforce. A Node-only suite cannot verify
# browser behaviour; only a real browser can.
#
=======
>>>>>>> 4a82a0f9fd43c3a236a9a06b6dc7bbccb5001c3f
# Note what is NOT here: a deploy step. Nothing in this repository is fit to
# deploy against real funds, and a pipeline that could do so would be an
# invitation to a mistake.

name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Typecheck, test, audit
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      # `npm ci` installs exactly the lockfile. `npm install` may silently
      # resolve a different version, which for a wallet's cryptographic
      # dependencies is a supply-chain risk, not a convenience.
      - name: Install (locked)
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Cryptography tests
        run: npx vitest run tests/cryptography/

      - name: Security tests
        run: npx vitest run tests/security/

      - name: Fuzz tests
        run: npx vitest run tests/fuzz/

      - name: API security tests
        run: npm run test:api

      - name: Unit tests
        run: npx vitest run tests/unit/

      # Runs separately so the summary line reports the true total, including
      # how many regtest tests were SKIPPED. A green build with consensus
      # validation skipped means less than one with it run.
      - name: Full suite
        run: npm test

      # Deployment config (Vercel-specific paths/env) is only meaningful on
      # the platform that will actually run it. Skip on non-Linux runners
      # rather than asserting something irrelevant to the target platform.
      - name: Validate deployment config
        if: matrix.os == 'ubuntu-latest'
        run: npm run check:vercel

      - name: Dependency audit
        run: npm audit --audit-level=moderate

  secrets:
    name: Secret scanning
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history: a secret removed in a later commit is still a secret

      - name: Scan for committed secrets
        run: |
          set -e
          echo "Checking for committed .env files..."
          if git ls-files | grep -E '^\.env$|^\.env\.[^e]'; then
            echo "::error::A .env file is committed"
            exit 1
          fi

          echo "Checking for seed or mnemonic files..."
          if git ls-files | grep -E '\.(seed|mnemonic)$|wallet-backup'; then
            echo "::error::A seed or mnemonic file is committed"
            exit 1
          fi

          # A 12- or 24-word BIP-39 phrase in source. Excludes the test files
          # that legitimately contain the published all-zeros test vector.
          echo "Checking for mnemonic phrases outside test fixtures..."
          if grep -rniE '(abandon[[:space:]]+){11}' \
               --include='*.ts' --include='*.js' --include='*.json' \
               --exclude-dir=node_modules . \
             | grep -vE '(tests/|api/tests/|scripts/|docs/)'; then
            echo "::error::A mnemonic phrase appears outside test fixtures"
            exit 1
          fi

          # 64 hex characters assigned to something key-shaped.
          echo "Checking for hard-coded private keys..."
          if grep -rniE '(private_?key|secret|seed)[[:space:]]*[:=][[:space:]]*["'"'"'][0-9a-f]{64}["'"'"']' \
               --include='*.ts' --include='*.js' \
               --exclude-dir=node_modules . \
             | grep -vE '(tests/|api/tests/)'; then
            echo "::error::A hard-coded 64-hex key appears outside test fixtures"
            exit 1
          fi

          echo "No committed secrets found."

  guards:
    name: Structural security guards
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci

      # These two guards cannot be expressed as behavioural tests: a
      # weak-entropy wallet works perfectly, and a timing-unsafe
      # implementation produces correct output. Only a source scan catches
      # them, and running this matrix across OSes is what would have caught
      # VEY-001 in CI instead of relying on a developer's own machine --
      # see docs/ATTACKS.md VEY-001.
      - name: No predictable randomness in core/
        run: npx vitest run tests/cryptography/entropy.test.ts

      - name: Reference implementations are isolated
        run: npx vitest run tests/cryptography/reference-implementations.test.ts

  e2e:
    name: Browser e2e (real engine)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        # VEY-020 was a `fetch` receiver-binding rule that only a real
        # browser's WebIDL enforces -- Node's undici and jsdom cannot see it.
        # The axis that matters here is ENGINE, not OS: Chromium and WebKit
        # brand-check the receiver, Firefox's behaviour differs, and a bug
        # that only breaks on one engine needs that engine actually running.
        browser: [chromium, firefox, webkit]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci

      - name: Install Playwright browser (${{ matrix.browser }})
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run e2e suite against ${{ matrix.browser }}
        run: npx playwright test --project=${{ matrix.browser }}

      - name: Upload trace on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-trace-${{ matrix.browser }}
          path: test-results/
          retention-days: 7